# yawop game runtime. (The dictionary pipeline has its own deps; see pipeline/.)
kivy>=2.3.0
# Shared UI library kivyshell is vendored as a git submodule at libs/kivyshell
# (pinned to v0.1.3), not pip-installed, so it also bundles into the Android APK.
# After cloning:  git submodule update --init
# To move to a newer kivyshell:  cd libs/kivyshell && git fetch && git checkout <tag>
